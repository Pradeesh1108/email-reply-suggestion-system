# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LLM provider
groq>=0.25.0

# Retrieval / similarity (CPU-only, no torch/GPU — see README §Cost-Control)
scikit-learn>=1.5.0

# Data persistence & validation
sqlmodel>=0.0.22
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0          # for FastAPI TestClient
